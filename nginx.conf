events {
    worker_connections 1024; # Настройка, которую можно подстроить под вашу систему
}

http {
    server {
        listen 80;

        location / {
            root /Users/mans/projects/Ai/project_ai/build;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }
    }
}
